<!DOCTYPE html>

<title>New Reservation</title>

{% block body %}
    <h1>Create new Reservation</h1>

    {{ include('general_reservation/_form.html.twig') }}

    <a href="{{ path('general_reservation_index') }}">back to list</a>
{% endblock %}
