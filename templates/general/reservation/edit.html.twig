<!DOCTYPE html>

<title>Edit Reservation</title>

{% block body %}
    <h1>Edit Reservation</h1>

    {{ include('general_reservation/_form.html.twig', {'button_label': 'Update'}) }}

    <a href="{{ path('general_reservation_index') }}">back to list</a>

    {{ include('general_reservation/_delete_form.html.twig') }}
{% endblock %}
