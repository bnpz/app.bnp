<!DOCTYPE html>

<title>New Contact</title>

{% block body %}
    <h1>Create new Contact</h1>

    {{ include('general/contact/_form.html.twig') }}

    <a href="{{ path('general_contact_index') }}">back to list</a>
{% endblock %}
