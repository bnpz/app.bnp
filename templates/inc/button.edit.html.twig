<a class="btn btn-lg btn-bnp btn-outline-info" href="{{ path(route, {'id': id}) }}">
    {% if label is defined and label is not null and label is not empty%}
        {{ label |trans }}
    {% else %}
        {{ 'label.edit' |trans }}
    {% endif %}
</a>