<nav class="navbar navbar-expand-sm navbar-dark bg-primary mb-3">
    {% if is_granted("ROLE_ADMIN") %}
    <a class="navbar-brand" href="{{ url("app_admin_admin_index") }}">Admin</a>
    {% endif %}
    <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarSupportedContent" aria-controls="navbarSupportedContent" aria-expanded="false" aria-label="Toggle navigation">
        <span class="navbar-toggler-icon"></span>
    </button>

    <div class="collapse navbar-collapse" id="navbarSupportedContent">
        <ul class="navbar-nav mr-auto">
            <li class="nav-item">
                <a class="nav-link" href="{{ url('homepage') }}">
                    {{ 'label.homePage'|trans }}
                    <span class="sr-only">(current)</span>
                </a>
            </li>

            {# ADRESAR #}
            <li class="nav-item dropdown">
                <a class="nav-link dropdown-toggle" href="#" id="navbarArchive" role="button" data-toggle="dropdown" aria-haspopup="true" aria-expanded="false">
                    {{ 'label.contacts'|trans }}
                </a>
                <div class="dropdown-menu dropdown-menu-left" aria-labelledby="navbarArchive">
                    <a class="dropdown-item" href="{{ url('general_contact_index') }}">
                        {{ 'label.addressBook'|trans }}
                    </a>
                    <div class="dropdown-divider"></div>
                    <a class="dropdown-item" href="{{ url('general_contact_new') }}">
                        {{ 'label.new'|trans }}
                    </a>
                </div>


            </li>

            {# EVENTS #}
            <li class="nav-item">
                <a class="nav-link" href="{{ url('general_event_index') }}">
                    {{ 'label.events'|trans }}
                    <span class="sr-only">(current)</span>
                </a>
            </li>
            {#<li class="nav-item dropdown">
                <a class="nav-link dropdown-toggle" href="#" id="navbarArchive" role="button" data-toggle="dropdown" aria-haspopup="true" aria-expanded="false">
                    {{ 'label.events'|trans }}
                </a>
                <div class="dropdown-menu dropdown-menu-left" aria-labelledby="navbarArchive">
                    <a class="dropdown-item" href="{{ url('general_event_index') }}">
                        {{ 'label.allEvents'|trans }}
                    </a>
                    <a class="dropdown-item" href="{{ url('general_event_index_new') }}">
                        {{ 'label.newEvents'|trans }}
                    </a>
                    <a class="dropdown-item" href="{{ url('general_event_index_old') }}">
                        {{ 'label.oldEvents'|trans }}
                    </a>
                    <div class="dropdown-divider"></div>
                    <a class="dropdown-item" href="{{ url('general_event_new') }}">
                        {{ 'label.new'|trans }}
                    </a>
                </div>
            </li>#}

            {# RESERAVTIONS #}
            {#<li class="nav-item dropdown">
                <a class="nav-link dropdown-toggle" href="#" id="navbarArchive" role="button" data-toggle="dropdown" aria-haspopup="true" aria-expanded="false">
                    {{ 'label.reservations'|trans }}
                </a>
                <div class="dropdown-menu dropdown-menu-left" aria-labelledby="navbarArchive">
                    <a class="dropdown-item" href="{{ url('general_reservation_index') }}">
                        {{ 'label.allReservations'|trans }}
                    </a>
                    <div class="dropdown-divider"></div>
                    <a class="dropdown-item" href="{{ url('general_reservation_new') }}">
                        {{ 'label.new'|trans }}
                    </a>
                </div>
            </li>#}
            {# ARHIVA #}
            {#<li class="nav-item dropdown">
                <a class="nav-link dropdown-toggle" href="#" id="navbarArchive" role="button" data-toggle="dropdown" aria-haspopup="true" aria-expanded="false">
                    Arhiva
                </a>
                <div class="dropdown-menu dropdown-menu-left" aria-labelledby="navbarArchive">
                    <a class="dropdown-item" href="{{ url('archive_index') }}">Početna</a>
                    <div class="dropdown-divider"></div>
                    <a class="dropdown-item" href="{{ url('archive_stages_index') }}">Scene</a>
                </div>


            </li>#}

        </ul>

        {# USER NAVBAR #}
        <ul class="navbar-nav ml-auto">
            <li class="nav-item dropdown">
                <a class="nav-link dropdown-toggle" href="#" id="navbarUser" role="button" data-toggle="dropdown" aria-haspopup="true" aria-expanded="false">
                    {{ app.user.name }}
                </a>
                <div class="dropdown-menu dropdown-menu-right" aria-labelledby="navbarUser">
                    {% if is_granted("ROLE_ADMIN") %}
                        <a class="dropdown-item" href="{{ url('app_admin_admin_account') }}">
                            {{ 'label.myAccount'|trans }}
                        </a>
                        <a class="dropdown-item" href="{{ url('app_admin_admin_docs') }}">
                            {{ 'label.documentation'|trans }}
                        </a>
                        <div class="dropdown-divider"></div>
                    {% endif %}

                    <a class="dropdown-item" href="{{ url('app_security_logout') }}">
                        {{ 'label.logout'|trans }}
                    </a>
                </div>
            </li>
        </ul>

    </div>
</nav>